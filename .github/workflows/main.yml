name: CI-CD 2nd project

on:
  push:
    branches: [ main ]
  pull_request: 
    branches: [ main ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Run make command
      run: make hello
    - name: Run echo command
      run: echo "Test for setup"
